version: '3.8'

services:
  motherhen:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./backend/downloads:/app/backend/downloads
      - ./backend/compressed:/app/backend/compressed
    environment:
      - FLASK_ENV=production
    restart: unless-stopped